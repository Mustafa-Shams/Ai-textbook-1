# Railway configuration file

[build]
builder = "NIXPACKS"

[nixpacks]
pythonVersion = "3.11"
poetry = false
pip = true

[build.args]
PYTHON_VERSION = "3.11"

[deploy]
startCommand = "cd backend && python -m uvicorn main:app --host 0.0.0.0 --port $PORT"